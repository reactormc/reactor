file(GLOB_RECURSE REACTOR_NET_SOURCES LIST_DIRECTORIES true net/*.c net/*.h)
add_library(REACTOR_NET STATIC ${REACTOR_NET_SOURCES})
target_include_directories(REACTOR_NET PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(REACTOR_NET PUBLIC REACTOR_UTIL)