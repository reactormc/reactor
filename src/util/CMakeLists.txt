file(GLOB_RECURSE REACTOR_UTIL_SOURCES LIST_DIRECTORIES true util/*.c util/*.h)
add_library(REACTOR_UTIL STATIC ${REACTOR_UTIL_SOURCES})
target_include_directories(REACTOR_UTIL PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})