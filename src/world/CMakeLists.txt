file(GLOB_RECURSE REACTOR_WORLD_SOURCES LIST_DIRECTORIES true world/*.c world/*.h)
add_library(REACTOR_WORLD STATIC ${REACTOR_WORLD_SOURCES})
target_include_directories(REACTOR_WORLD PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(REACTOR_WORLD PUBLIC REACTOR_UTIL)